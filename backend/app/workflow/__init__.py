"""LangGraph workflow engine — nodes, state, checkpointer, runner.

This package is the heart of Argus: it turns a session (company, website,
objective) into a structured research report via a multi-node LangGraph with
shared state, conditional routing, retries and a Mongo-backed checkpointer.
"""
